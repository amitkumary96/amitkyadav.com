---
import BaseLayout from '../layouts/BaseLayout.astro';
const posts = Object.values(import.meta.glob('../content/engineer/*.{md,mdx}', { eager: true })).sort((a,b)=> new Date(b.frontmatter.date) - new Date(a.frontmatter.date));
---
<BaseLayout title="Engineer - Amit Kumar Yadav">
  <h1 class="text-2xl font-bold mb-4">Engineer</h1>
  <p class="mb-4 text-gray-600">Technical posts. Use Markdown files under <code>src/content/engineer/</code>.</p>
  <ul class="space-y-4">
    {posts.length ? posts.map(p => (
      <li class="p-3 border rounded" key={p.frontmatter.title}>
        <a href={p.url} class="font-semibold">{p.frontmatter.title}</a>
        <div class="text-sm text-gray-600">{p.frontmatter.excerpt}</div>
      </li>
    )) : <li class="text-gray-600">No posts yet.</li>}
  </ul>
</BaseLayout>
