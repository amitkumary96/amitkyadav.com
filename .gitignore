# Dependencies
node_modules

# Build output
dist

# Astro cache and generated files
.astro

# Environment variables
.env
.env.local
.env.*.local

# IDE and editors
.vscode
.idea
*.swp
*.swo
*.code-workspace
.project

# OS files
.DS_Store
Thumbs.db
Desktop.ini
.AppleDouble
.LSOverride
*~

# Logs
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# NOTE: public/uploads is deliberately NOT ignored. It was, and that silently
# discarded every CMS image upload — the file landed on disk, git skipped it,
# and the deploy shipped a broken image with no error anywhere.

# Private keys (deployment)
id_rsa_amitkyadav
id_rsa_amitkyadav.pub
