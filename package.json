{
  "name": "amitkyadav.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-sitemap.js && node scripts/generate-rss.js",
    "preview": "astro preview",
    "format": "prettier --write .",
    "cms": "npx decap-server",
    "postinstall": "echo 'Run npm run dev to start the dev server. Access CMS at http://localhost:4321/admin'"
  },
  "dependencies": {
    "astro": "^4.15.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "prettier": "^2.8.8",
    "rss": "^1.2.2",
    "tailwindcss": "^3.4.17"
  }
}
